# 反代内核运行时接入指南

本文约束已定型反代内核如何从“仓库参考”进入“可运行执行器”状态。

## 原则

1. 优先使用 release 中已经构建好的二进制或压缩包。
2. 完整仓库源码只在 release 不存在、能力不透明、需要协议参考或需要重写 adapter 时拉到 `source-repo/`。
3. 受控执行器必须固定版本、固定 SHA256。
4. 受控执行器只能监听 `127.0.0.1`、`localhost` 或 `::1`。
5. 不能把第三方反代项目作为公网长期服务直接暴露给用户。
6. 平台仍然负责公开 API、账号池、用户 API Key、审计、计费和资产入库。

## 管理接口

| 接口 | 用途 |
|---|---|
| `GET /v1/admin/proxy-kernels` | 查看全部定型内核、账号状态、runtime 状态和阻塞项 |
| `GET /v1/admin/proxy-kernels/routing-plan` | 查看全部定型内核的 provider、模型映射、账号/runtime 缺口 |
| `GET /v1/admin/proxy-kernels/runtime-delivery-plan` | 查看全部定型内核从 release、SHA256、loopback runtime、source-repo 兜底到账号导入的下一步 |
| `GET /v1/admin/proxy-kernels/release-probe-matrix` | 批量探测定型仓库 release 状态、候选资产、hash 缺口和 source-repo 兜底原因；`dry_run=true` 不访问 GitHub |
| `GET /v1/admin/proxy-kernels/runtime-contract-matrix` | 查看全部定型内核的 loopback 调用合同、图片/视频端点覆盖、账号引用头、轮询与输出解析规则 |
| `GET /v1/admin/proxy-kernels/production-readiness-matrix` | 汇总路由、运行合同、runtime、账号、健康检查和真实样本验收，判断是否可进生产 |
| `POST /v1/admin/proxy-kernels/apply-routing` | 批量初始化全部或指定定型内核的 provider 与模型映射；不创建账号 |
| `GET /v1/admin/proxy-kernels/go-live-checklist` | 查看全部定型内核从路由、runtime、账号到真实样本验收的只读上线清单 |
| `GET /v1/admin/proxy-kernels/materials-request` | 查看全部定型内核还需要操作者提供哪些账号、release/runtime 和验收材料 |
| `POST /v1/admin/proxy-kernels/loopback-contract-test` | 启动临时本机 runner，验证平台到 loopback 反代内核的通用图片/视频转发合同 |
| `GET /v1/admin/proxy-kernels/{provider_id}` | 查看单个 provider 的内核状态 |
| `GET /v1/admin/proxy-kernels/{provider_id}/runtime-delivery-plan` | 查看单个 provider 的 release 优先计划、命令模板、当前证据和下一步 |
| `GET /v1/admin/proxy-kernels/{provider_id}/runtime-contract` | 查看单个 provider 的标准请求、账号引用、轮询、输出入库和 `/v1/images/*`、`/v1/videos/*` 端点合同 |
| `GET /v1/admin/proxy-kernels/{provider_id}/production-readiness` | 查看单个 provider 的生产就绪证据、阻塞阶段、下一步命令和真实样本验收覆盖 |
| `GET /v1/admin/proxy-kernels/{provider_id}/go-live-checklist` | 查看单个 provider 的下一步、必填账号材料和可执行命令 |
| `GET /v1/admin/proxy-kernels/{provider_id}/materials-request` | 查看单个 provider 的材料准备清单；只读、不调上游、不消耗额度 |
| `POST /v1/admin/proxy-kernels/{provider_id}/release-probe` | 从 GitHub release 探测可用资产 |
| `POST /v1/admin/proxy-kernels/{provider_id}/install-release` | 下载并校验指定 release 资产 |
| `GET /v1/admin/proxy-kernels/{provider_id}/routing-plan` | 查看模板映射、已启用映射、账号/runtime 阻塞项和下一步 |
| `POST /v1/admin/proxy-kernels/{provider_id}/apply-routing` | 初始化 provider 与模型映射；不创建账号、不写入假凭据 |
| `POST /v1/admin/proxy-kernels/{provider_id}/register-runtime` | 登记已启动的 loopback runtime，并同步 provider `base_url` |
| `POST /v1/admin/proxy-kernels/{provider_id}/start-runtime` | 使用已校验资产启动受控本地执行器，并同步 provider `base_url` |
| `POST /v1/admin/proxy-kernels/{provider_id}/stop-runtime` | 停止平台启动的受控执行器 |
| `GET /v1/admin/proxy-kernels/{provider_id}/process` | 查看受控执行器 PID、命令、日志路径和运行状态 |
| `GET /v1/admin/proxy-kernels/{provider_id}/logs` | 查看 stdout/stderr 最新日志 |
| `POST /v1/admin/proxy-kernels/{provider_id}/clear-runtime` | 停止受控执行器，清除 runtime 登记和 provider `base_url` |
| `GET /v1/admin/proxy-kernels/{provider_id}/source-repo` | 查看定型仓库源码参考的本地状态 |
| `POST /v1/admin/proxy-kernels/{provider_id}/source-repo/sync` | 将定型仓库同步到 `source-repo/`，仅用于协议参考、本地构建或 adapter 重写 |

管理后台也提供同样的可视化路径：

1. 打开 `/admin`。
2. 进入“反代内核”。
3. 在“运行状态”查看定型内核、账号/runtime/blocker。
4. 在“运行状态”点击“查看全部路由计划”，确认全部定型 provider 还缺什么。
5. 点击“补齐全部定型路由”批量创建 provider 与模型映射；它不会创建假账号。
6. 点击“查看运行时交付计划”，确认每个 provider 当前下一步是探测 release、填写 SHA256、启动 loopback、同步 source-repo 还是导入账号。
7. 点击“全量 Release 探测”，确认哪些仓库已有可优先安装的 release 资产，哪些仓库需要 source-repo 兜底。
8. 点击“运行合同矩阵”，确认每个 provider 的图片/视频提交端点、账号引用头、轮询路径和输出解析规则都可由平台适配。
9. 点击“生产就绪矩阵”，查看每个 provider 当前卡在路由、runtime、账号、健康检查还是真实样本验收。
10. 点击“查看全部材料清单”，先确认每个 provider 需要你提供哪些账号/session、release、SHA256、loopback 地址和验收材料。
11. 点击“Loopback 合同自检”，先验证平台自身能把图片/视频请求完整转发给本机反代内核并完成资产入库。
12. 点击“查看全部上线清单”，确认每个 provider 下一步到底是补路由、装 runtime、导账号还是跑真实样本。
13. 在“启动执行器”先探测 release，填写 asset 名称和 SHA256 后点击“安装 Release”。
14. 安装成功后，页面会自动填入资产路径和 SHA256；再填写 loopback base URL 和启动命令。
15. 点击“查看路由计划”确认单个 provider 还缺账号、runtime 还是映射；点击“补齐路由映射”只会创建该 provider 的 `ProviderModelMapping`。
16. 点击“运行合同”读取单个 provider 的标准请求、账号引用头、轮询和输出入库合同。
17. 点击“生产就绪”读取单个 provider 的阶段证据、最近健康检查和真实样本覆盖。
18. 点击“查看材料清单”读取单个 provider 的 `required_materials`、账号字段、runtime 字段和可执行 curl。
19. 点击“查看上线清单”读取单个 provider 的账号材料、runtime 和验收命令。
20. 在“日志与停止”查看 stdout/stderr、停止 runtime 或清除登记。
21. 只有 release 资产不可用或需要协议参考时，进入“源码参考”同步定型仓库。

## 安装 release

安装时必须提供 `expected_sha256`。没有 hash 的 release 资产只能探测，不能登记为已校验安装。

先用 dry-run 确认本次会覆盖哪些定型仓库；它不访问 GitHub、不下载资产：

```bash
curl "$MEDIA2API_BASE_URL/v1/admin/proxy-kernels/release-probe-matrix?dry_run=true" \
  -H "Authorization: Bearer $MEDIA2API_API_KEY"
```

真实批量探测会访问各定型仓库的 GitHub release，但仍然只读、不下载、不启动进程：

```bash
curl "$MEDIA2API_BASE_URL/v1/admin/proxy-kernels/release-probe-matrix" \
  -H "Authorization: Bearer $MEDIA2API_API_KEY"
```

响应中每行的 `install_ready=true` 只代表找到候选 release 资产；实际安装仍必须由操作者选择 `asset_name` 并提供可信来源的 `expected_sha256`。

## 运行合同矩阵

运行合同矩阵用于回答“平台会如何调用这个反代内核”。它不访问上游、不下载 release、不启动进程。

```bash
curl "$MEDIA2API_BASE_URL/v1/admin/proxy-kernels/runtime-contract-matrix" \
  -H "Authorization: Bearer $MEDIA2API_API_KEY"

curl "$MEDIA2API_BASE_URL/v1/admin/proxy-kernels/openai_web_session/runtime-contract" \
  -H "Authorization: Bearer $MEDIA2API_API_KEY"
```

关键字段：

- `adapter_contract.endpoints`: 每个 operation 实际提交到 loopback runner 的路径。
- `adapter_contract.task_id_paths`、`status_paths`、`output_paths`: 异步任务 ID、状态和输出资产的解析规则。
- `auth_contract.header_names`: 平台传给 runner 的账号引用头。
- `checks`: 合同是否覆盖 http adapter、端点映射、`/v1/images/*`、`/v1/videos/*`、loopback-only 和禁止官方 SDK/API。
- `state`: 路由、runtime、账号、hash 的当前证据。

## 生产就绪矩阵

生产就绪矩阵用于回答“这个 provider 是否可以直接承接真实用户图片/视频请求”。它只读取当前证据，不自动运行上游调用。

```bash
curl "$MEDIA2API_BASE_URL/v1/admin/proxy-kernels/production-readiness-matrix" \
  -H "Authorization: Bearer $MEDIA2API_API_KEY"

curl "$MEDIA2API_BASE_URL/v1/admin/proxy-kernels/openai_web_session/production-readiness" \
  -H "Authorization: Bearer $MEDIA2API_API_KEY"
```

必须同时满足：

- `routing`: provider 和模型映射已启用。
- `runtime_contract`: 平台到 loopback runner 的调用合同 ready。
- `runtime`: 已登记或启动 loopback runtime，且受控进程未停止。
- `release_or_runtime_artifact`: release/hash 已校验，或已有可追溯 runtime 登记。
- `account`: 至少一个真实可用账号材料已导入。
- `health`: 最近一次 provider health-check 通过。
- `live_acceptance`: 声明操作已有真实 completed job 和可下载资产证据。

```bash
curl -X POST "$MEDIA2API_BASE_URL/v1/admin/proxy-kernels/openai_web_session/release-probe" \
  -H "Authorization: Bearer $MEDIA2API_API_KEY"
```

```bash
curl -X POST "$MEDIA2API_BASE_URL/v1/admin/proxy-kernels/openai_web_session/install-release" \
  -H "Authorization: Bearer $MEDIA2API_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "tag_name": "vX.Y.Z",
    "asset_name": "example-linux-amd64.tar.gz",
    "expected_sha256": "64位十六进制sha256"
  }'
```

## 登记 loopback runtime

当你已经用固定版本/hash 的 release 二进制在服务器本地启动执行器后，登记它：

```bash
curl -X POST "$MEDIA2API_BASE_URL/v1/admin/proxy-kernels/openai_web_session/register-runtime" \
  -H "Authorization: Bearer $MEDIA2API_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "base_url": "http://127.0.0.1:19081",
    "version": "vX.Y.Z",
    "binary_path": "/opt/media2api/var/proxy-kernels/openai_web_session/vX.Y.Z/example-linux-amd64",
    "sha256": "64位十六进制sha256",
    "notes": "OAI-WEB-01 verified release"
  }'
```

`register-runtime` 会拒绝非 loopback 地址。通过后，平台会同步更新该 provider 的 `base_config.base_url`，后续能力同步、health check 和媒体调用才会走这个受控执行器。

## 路由映射

从零清空数据后，平台不会自动生成假账号。执行器安装/登记之后，可以先查看路由计划：

```bash
curl "$MEDIA2API_BASE_URL/v1/admin/proxy-kernels/routing-plan" \
  -H "Authorization: Bearer $MEDIA2API_API_KEY"
```

批量应用全部定型内核的路由：

```bash
curl -X POST "$MEDIA2API_BASE_URL/v1/admin/proxy-kernels/apply-routing" \
  -H "Authorization: Bearer $MEDIA2API_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "provider_ids": [],
    "status": "active",
    "enable_mappings": true,
    "priority_offset": 0,
    "update_provider_base_url": true
  }'
```

如果只想检查单个 provider：

```bash
curl "$MEDIA2API_BASE_URL/v1/admin/proxy-kernels/openai_web_session/routing-plan" \
  -H "Authorization: Bearer $MEDIA2API_API_KEY"
```

如果 `missing_mappings` 或 `NO_ENABLED_MAPPINGS` 存在，可以应用定型模板映射：

```bash
curl -X POST "$MEDIA2API_BASE_URL/v1/admin/proxy-kernels/openai_web_session/apply-routing" \
  -H "Authorization: Bearer $MEDIA2API_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "status": "active",
    "enable_mappings": true,
    "priority_offset": 0,
    "update_provider_base_url": true
  }'
```

该接口只做三件事：初始化 provider、合并模板配置、创建或更新模板中的模型映射。它不会导入 Web Cookie、OAuth profile、API Key，也不会创建默认账号。`NO_ACTIVE_ACCOUNT` 仍然表示必须到“授权资源/账号池”导入真实账号材料。

## 材料准备清单

材料准备清单是只读接口，用来回答“要把这个 provider 接起来，我现在到底还需要给系统什么”。它不会创建 provider、不会导入账号、不会启动 runtime、不会调用上游，也不会消耗任何账号额度。

查看全部定型 provider：

```bash
curl "$MEDIA2API_BASE_URL/v1/admin/proxy-kernels/materials-request" \
  -H "Authorization: Bearer $MEDIA2API_API_KEY"
```

查看单个 provider：

```bash
curl "$MEDIA2API_BASE_URL/v1/admin/proxy-kernels/openai_web_session/materials-request" \
  -H "Authorization: Bearer $MEDIA2API_API_KEY"
```

返回重点字段：

| 字段 | 含义 |
|---|---|
| `required_materials` | 当前真正缺少的材料；区分用户必须提供和平台可自动生成 |
| `message_to_user` | 给操作者看的最短行动清单 |
| `account_materials` | 账号授权材料，如 Web cookie/session、Codex profile、Gemini CLI OAuth、Discord guild/channel |
| `runtime_materials` | release asset、SHA256、loopback base URL、启动命令和 `source-repo/` 兜底策略 |
| `routing_materials` | provider/model mapping 是否已准备；平台可通过 `apply-routing` 生成 |
| `validation_materials` | 真实样本验收需要的 admin key、user key、样本模型和操作范围 |

材料清单与上线清单的区别：

- 材料清单面向操作者，告诉你“请提供哪些东西”。
- 上线清单面向验收，告诉你“这些东西是否已经满足上线前置条件”。
- 两者都不使用官方 SDK/API，不调用上游，不创建假账号。

## Loopback 合同自检

Loopback 合同自检用于验证平台自身对反代内核的通用调用合同：临时启动一个只监听 `127.0.0.1` 的 runner，临时创建 provider、账号、模型映射和 pricing rule，然后实际跑 `text_to_image`、`image_edit`、`text_to_video` 三类任务。通过后说明平台调度、账号租约、`http_adapter` 转发、返回资产解析和资产入库链路是闭环的。

```bash
curl -X POST "$MEDIA2API_BASE_URL/v1/admin/proxy-kernels/loopback-contract-test" \
  -H "Authorization: Bearer $MEDIA2API_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"operations":["text_to_image","image_edit","text_to_video"]}'
```

该自检的边界：

- 它不调用任何真实上游平台，也不使用官方 SDK/API。
- 它不替代 OpenAI/Gemini/豆包/Qwen 等真实账号验收。
- 它只证明“平台 -> loopback 反代内核 -> 平台资产入库”这条通用合同可用。
- 运行结束后临时 provider、账号、模型映射和 pricing rule 会被禁用，不作为生产路由使用。

## 上线清单

上线清单是只读排查接口，不会启动 runtime、不会调用上游、不会消耗额度。它把路由、release/hash、loopback runtime、账号材料和真实样本验收命令合成一个页面。

查看全部定型 provider：

```bash
curl "$MEDIA2API_BASE_URL/v1/admin/proxy-kernels/go-live-checklist" \
  -H "Authorization: Bearer $MEDIA2API_API_KEY"
```

查看单个 provider：

```bash
curl "$MEDIA2API_BASE_URL/v1/admin/proxy-kernels/openai_web_session/go-live-checklist" \
  -H "Authorization: Bearer $MEDIA2API_API_KEY"
```

返回中的 `steps` 会按顺序显示：

| step | 含义 |
|---|---|
| `routing` | provider 与模型映射是否已准备 |
| `runtime` | loopback runtime 是否已登记并限制在本机 |
| `release_asset` | release 资产或 runtime hash 是否有校验证据 |
| `account` | 是否已有真实 active 账号材料 |
| `live_acceptance` | 是否可以开始运行真实样本验收 |

## 源码参考

源码不是默认运行路径。只有以下情况才同步到 `source-repo/`：

1. GitHub release 没有可用二进制。
2. release asset 不完整或无法确认媒体路由能力。
3. 需要确认 cookie/session、CLI OAuth、任务轮询或资产下载协议。
4. 需要参考逻辑重写平台内 adapter。

查看状态：

```bash
curl "$MEDIA2API_BASE_URL/v1/admin/proxy-kernels/openai_web_session/source-repo" \
  -H "Authorization: Bearer $MEDIA2API_API_KEY"
```

同步定型仓库：

```bash
curl -X POST "$MEDIA2API_BASE_URL/v1/admin/proxy-kernels/openai_web_session/source-repo/sync" \
  -H "Authorization: Bearer $MEDIA2API_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"ref":"","force":false}'
```

平台只允许同步内核选型表中的仓库，路径固定为 `source-repo/<owner>__<repo>`。如果目录已存在且不是同一个 GitHub 远端，会拒绝同步。

## 受控启动 loopback runtime

如果 release 资产已经放在 `MEDIA2API_PROXY_KERNEL_DIR` 下，并且你掌握该资产的 SHA256，可以让平台直接启动它。启动命令必须是参数数组，不能走 shell；`artifact_path` 必须出现在 `command` 中，避免记录的 hash 和实际启动文件不一致。

```bash
curl -X POST "$MEDIA2API_BASE_URL/v1/admin/proxy-kernels/openai_web_session/start-runtime" \
  -H "Authorization: Bearer $MEDIA2API_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "command": [
      "/opt/media2api/var/proxy-kernels/openai_web_session/vX.Y.Z/example-linux-amd64",
      "--host",
      "127.0.0.1",
      "--port",
      "19081"
    ],
    "base_url": "http://127.0.0.1:19081",
    "artifact_path": "/opt/media2api/var/proxy-kernels/openai_web_session/vX.Y.Z/example-linux-amd64",
    "expected_sha256": "64位十六进制sha256",
    "version": "vX.Y.Z",
    "replace_existing": true
  }'
```

常用检查：

```bash
curl "$MEDIA2API_BASE_URL/v1/admin/proxy-kernels/openai_web_session/process" \
  -H "Authorization: Bearer $MEDIA2API_API_KEY"

curl "$MEDIA2API_BASE_URL/v1/admin/proxy-kernels/openai_web_session/logs?stream=stderr&max_bytes=12000" \
  -H "Authorization: Bearer $MEDIA2API_API_KEY"
```

停止受控执行器：

```bash
curl -X POST "$MEDIA2API_BASE_URL/v1/admin/proxy-kernels/openai_web_session/stop-runtime" \
  -H "Authorization: Bearer $MEDIA2API_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"grace_seconds":5}'
```

## 状态解释

`GET /v1/admin/proxy-kernels` 的 `blockers` 字段用于告诉用户还缺什么：

| blocker | 含义 |
|---|---|
| `PROVIDER_NOT_INITIALIZED` | 还没有通过账号导入或模板初始化 provider |
| `PROVIDER_DISABLED` | provider 已存在但未启用，暂不会参与路由 |
| `NO_ENABLED_MAPPINGS` | 还没有启用该 provider 的模板模型映射 |
| `TEMPLATE_MAPPINGS_INCOMPLETE` | 定型模板中还有部分模型映射未写入 |
| `NO_ACTIVE_ACCOUNT` | 还没有导入真实可用账号材料 |
| `NO_LOOPBACK_RUNTIME` | 还没有登记受控 loopback 执行器或完成原生 adapter 重写 |
| `KERNEL_HASH_NOT_VERIFIED` | 已安装资产的 SHA256 与 expected SHA256 不一致 |
| `KERNEL_PROCESS_STOPPED` | 已由平台登记受控进程，但当前进程未运行 |

只有 provider、账号、runtime/hash 与真实样本验收都通过后，才能把该 provider 标记为生产可用。

## 运行时交付计划

运行时交付计划是只读接口，用来回答“这个反代内核现在到底卡在哪一步”。它不会下载 release、不会启动进程、不会同步源码、不会创建账号，也不会调用上游。

查看全部 provider：

```bash
curl "$MEDIA2API_BASE_URL/v1/admin/proxy-kernels/runtime-delivery-plan" \
  -H "Authorization: Bearer $MEDIA2API_API_KEY"
```

查看单个 provider：

```bash
curl "$MEDIA2API_BASE_URL/v1/admin/proxy-kernels/openai_web_session/runtime-delivery-plan" \
  -H "Authorization: Bearer $MEDIA2API_API_KEY"
```

重点字段：

| 字段 | 含义 |
|---|---|
| `next_step` | 当前最该执行的一步，例如 `apply_routing`、`probe_release`、`install_release`、`start_runtime`、`import_account`、`live_acceptance` |
| `state` | 当前证据：路由、release、hash、runtime、进程、账号、source-repo 是否已具备 |
| `runtime.install_payload_template` | 安装 release 时需要填写的 `tag_name`、`asset_name`、`expected_sha256` 模板 |
| `runtime.start_payload_template` | 启动 loopback runtime 的命令数组、base URL、artifact path 和 SHA256 模板 |
| `runtime.source_repo_payload_template` | 只有 release 不可用或需要协议参考时，才使用的 source-repo 同步模板 |
| `commands` | 对应 curl 命令模板，便于复制执行或对照后台表单 |

推荐顺序：

1. `apply_routing`：先把 provider 和模型映射补齐。
2. `probe_release`：优先探测 GitHub release。
3. `install_release`：填入 release asset 名称和 64 位 SHA256，安装并校验。
4. `start_runtime`：用已校验资产启动只监听 loopback 的执行器。
5. `import_account`：去“授权资源/账号池”导入真实 Web session 或 Agent Provider 材料。
6. `live_acceptance`：运行真实样本验收，确认图片/视频资产真的入库。
